services:
  # for PC's1
  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
  # TODO: implement replicas

  # for PC2 (not needed for now)
  # mysql:
  #   image: mysql:latest
  #   container_name: mysql
  #   ports:
  #     - "3306:3306"
  #   environment:
  #     MYSQL_ROOT_PASSWORD: root
  #   volumes:
  #     - mysql_data:/var/lib/mysql


volumes:
  mongo_data:
  # mysql_data:
